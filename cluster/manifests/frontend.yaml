apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: rca-demo
data:
  default.conf: |
    server {
        listen 80;
        resolver kube-dns.kube-system.svc.cluster.local valid=5s;
        set $upstream http://checkout.rca-demo.svc.cluster.local;
        location / {
            proxy_pass $upstream;
            proxy_connect_timeout 5s;
            proxy_read_timeout 5s;
        }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: rca-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: nginx:alpine
        ports:
        - containerPort: 80
        volumeMounts:
        - name: config-volume
          mountPath: /etc/nginx/conf.d
        resources:
          requests:
            cpu: "50m"
            memory: "32Mi"
          limits:
            cpu: "100m"
            memory: "64Mi"
      volumes:
      - name: config-volume
        configMap:
          name: frontend-config
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: rca-demo
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
